{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "cd anon-feed-ui && npm install && npx tsc && npx vite build --mode preprod",
  "outputDirectory": "anon-feed-ui/dist",
  "devCommand": "cd anon-feed-ui && npm run dev",
  "installCommand": "npm install && cd anon-feed-ui && npm install",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ],
  "env": {
    "VITE_NETWORK_ID": "preprod",
    "VITE_LOGGING_LEVEL": "info",
    "VITE_CONTRACT_ADDRESS": "placeholder"
  },
  "ignoreCommand": "git diff HEAD^ HEAD --quiet -- anon-feed-ui/ contract/ api/"
}
